<Project>
    <PropertyGroup>
        <GetPackageVersionDependsOn>
            GitSemVersion;
            $(GetPackageVersionDependsOn)
        </GetPackageVersionDependsOn>
    </PropertyGroup>

    <Target
        Name="EnsureEditorConfig"
        BeforeTargets="Compile">
      <EnsureEditorConfigTask/>
    </Target>
</Project>